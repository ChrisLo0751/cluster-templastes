apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: oauth2-proxy
  namespace: argocd
spec:
  project: {{.Runtime.Name}}-runtime-project
  source:
    chart: oauth2-proxy
    repoURL: https://oauth2-proxy.github.io/manifests
    targetRevision: 6.12.0
    helm:
      values: |-
        config:
          clientID: {{.OauthConfigs.ClientID}}
          clientSecret: {{.OauthConfigs.ClientSecret}}

        extraArgs:
          provider: "oidc"
          provider-display-name: "Dex"
          whitelist-domain: ".10.204.118.214.nip.io:31443"
          cookie-secure: "true"
          cookie-domain: ".10.204.118.214.nip.io"
          redirect-url: {{printf "https://auth.%s.%s.nip.io:%s/oauth2/callback" .Runtime.Name .Runtime.Apiserver .Traefik.HttpsNodePort}}
          cookie-secret: "0d6a5698b9d52e612a6cb7161e747817"
          oidc-issuer-url: "{{.OauthConfigs.Issuer}}"
          scope: "openid profile email groups"
          email-domain: "*"
          skip-provider-button: "false"
          set-authorization-header: "true"

        ingress:
          enabled: true
          annotations:
            kubernetes.io/ingress.class: traefik
          tls:
            - hosts:
                - {{printf "auth.%s.%s.nip.io" .Runtime.Name .Runtime.Apiserver}}
          path: /
          pathType: ImplementationSpecific
          hosts:
            - {{printf "auth.%s.%s.nip.io" .Runtime.Name .Runtime.Apiserver}}

        extraVolumes:
          - name: ca-bundle-cert
            secret:
              secretName: idp-ca-secret

        extraVolumeMounts:
          - mountPath: /etc/ssl/certs/
            name: ca-bundle-cert
  destination:
    server: https://kubernetes.default.svc
    namespace: oauth2-proxy
  syncPolicy:
    automated:
      selfHeal: true
      prune: true
    syncOptions:
      - CreateNamespace=true
